name: CI/CD Pipeline

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
<<<<<<< Updated upstream
      - name: Run tests
        run: npm test -- --run
      
      - name: Run tests with coverage
        if: github.event_name == 'pull_request'
        run: npm run test:coverage
=======
      - name: Check for vulnerabilities
        run: npm audit --audit-level=moderate || true
      
      - name: Lint code
        run: npm run lint
      
      - name: Run tests
        run: npm run test:run
      
      - name: Run tests with coverage
        if: github.event_name == 'pull_request'
        run: npm run test:run -- --coverage
>>>>>>> Stashed changes

  deploy-prod:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment:
      name: production
      url: https://espruar.com
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
<<<<<<< Updated upstream
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run tests
        run: npm test -- --run
      
=======
>>>>>>> Stashed changes
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        if: github.ref == 'refs/heads/main'
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./
          cname: espruar.com

  deploy-dev:
    name: Deploy to Development
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/develop' && github.event_name == 'push'
    environment:
      name: development
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
<<<<<<< Updated upstream
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run tests
        run: npm test -- --run
      
=======
>>>>>>> Stashed changes
      # Note: Development deployment would go to a separate branch or environment
      # Uncomment and configure if you want a separate dev site
      # - name: Deploy to Development Site
      #   uses: peaceiris/actions-gh-pages@v3
      #   with:
      #     github_token: ${{ secrets.GITHUB_TOKEN }}
      #     publish_dir: ./
      #     publish_branch: gh-pages-dev
      #     cname: dev.espruar.com  # If you have a dev subdomain

